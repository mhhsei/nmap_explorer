# NMap Explorer 系統深度體檢與優化建議報告

> 審計日期：2026-08-30  
> 審計範疇：Android 原生層 (Kotlin)、後端嵌入式服務 (Chaquopy Python)、前端狀態機與語音渲染 (HTML5/JS)  
> 核心目標：在**不改動現有代碼**的前提下，針對「安全性 (Safety)」、「精確度 (Precision)」、「可靠性 (Reliability)」進行全面盤點，找出潛在盲區並提供明確升級方向。

---

## 目錄

1. [執行摘要](#執行摘要)
2. [第一部分：安全性 (Safety) - 攸關視障者人身安全](#第一部分安全性-safety---攸關視障者人身安全)
3. [第二部分：精確度 (Precision) - 定位、幾何與狀態機](#第二部分精確度-precision---定位幾何與狀態機)
4. [第三部分：可靠性 (Reliability) - 防閃退、生命週期與效能](#第三部分可靠性-reliability---防閃退生命週期與效能)
5. [第四部分：建議修復優先級與升級路線圖](#第四部分建議修復優先級與升級路線圖)

---

## 執行摘要

本次審計針對 7 大核心模組（卡爾曼濾波、空間世界模型、前端語音與狀態機、路口分析器、台灣號誌庫、原生 Android 架構、氣壓垂直高程）進行全面逐行分析。

整體架構在「完全無 C 擴展相容性」與「離線優先無障礙設計」上極具巧思，但同時也發現了數個**對視障者有直接人身危害**的關鍵邏輯（例如：離線時虛構綠燈倒數秒數、號誌雷達半徑過大造成巷弄誤報、行人左右側人行道吸附顛倒），以及導致導航卡死、定位隨時間發散的演算法漏洞。

---

## 第一部分：安全性 (Safety) - 攸關視障者人身安全

### 1. 致命缺陷：無即時連線時「虛構」紅綠燈秒數
* **相關檔案**：`app/src/main/python/nmap/spatial/taiwan_signals.py` (行 398-413)
* **問題描述**：當號誌標記為連網號誌（`is_connected_spat=True`），但當下並無即時連線快取資料時，系統竟以 `time.time() % cycle`（當前系統時間對燈號週期取餘數）推算剩餘秒數，並透過 TTS 朗讀「綠燈剩 X 秒」。
* **白話比喻**：就像導盲犬看不清前方號誌，卻隨機猜測「現在是綠燈，還有 8 秒可以過」，這會誘導視障者在真實紅燈時走入車道，屬於致命的假安全信號。
* **改進建議**：無即時連線推播時，嚴禁任何秒數猜測，僅播報「前方有號誌路口」，並將 `has_live_seconds` 強制設為 `False`。

### 2. 極度危險：號誌雷達半徑過大引發「跨路口污染」
* **相關檔案**：`app/src/main/python/nmap/spatial/world_model.py` (行 328-362)
* **問題描述**：系統以 32 公尺半徑搜尋紅綠燈與 APS 有聲號誌。但在台灣市區，平行巷弄間距常僅 15-20 公尺。
* **白話比喻**：視障者走在沒有紅綠燈的小巷口，但系統的雷達抓到了 25 公尺外大馬路上的「布穀鳥有聲號誌」，誤報「前方有聲號誌，請依布穀鳥聲通行」，容易造成使用者誤判路況而直接橫越無防護巷口。
* **改進建議**：
  1. 無號誌巷弄的搜尋半徑應緊縮至 15 公尺以內。
  2. 嚴格比對號誌所屬道路名稱與當前行進道路名稱，路名不符時嚴禁跨路口融合。

### 3. 危險：道路左右側吸附完全顛倒
* **相關檔案**：`app/src/main/python/nmap/spatial/pure_geometry.py` (行 180-207)
* **問題描述**：計算行人位於道路左側或右側時，使用的是地圖線段向量的外積。但 OpenStreetMap 繪製道路線段的方向是隨機的（可能由南向北，也可能由北向南），演算法未傳入使用者的 `heading_deg`（行進朝向）。
* **白話比喻**：不管視障者是往南走還是往北走，系統都以地圖作者畫線的方向當作基準。當使用者掉頭折返時，系統會把使用者瞬間「吸附」並拋到對向車道的人行道上，左右側播報徹底顛倒。
* **改進建議**：計算左右側前，先檢查線段向量與使用者行進朝向的夾角，若反向則將線段向量翻轉 180 度，確保左右判定完全貼合視障者真實視角。

### 4. 盲區缺陷：防撞雷達在最後 1.5 公尺停止警報
* **相關檔案**：`app/src/main/python/web/app.js` (行 2219-2248)
* **問題描述**：變電箱、施工等障礙物防撞警報條件設為 `distance <= 8.0 && distance >= 1.5`；有聲號誌提示設為 `distance <= 22.0 && distance >= 4.0`。
* **白話比喻**：當你距離變電箱 2 公尺時語音大聲警告；但當你只剩 1.2 公尺（即將撞上）時，因為小於 1.5 公尺，警告聲反而瞬間完全安靜。
* **改進建議**：防撞雷達下限應設為 `0.0m`；小於 1.5 公尺時應改為持續緊急逼逼聲或震動，絕不可進入靜音盲區。

---

## 第二部分：精確度 (Precision) - 定位、幾何與狀態機

### 1. 嚴重：卡爾曼濾波器「速度協方差」無限發散
* **相關檔案**：`app/src/main/java/com/example/nmapexplorer/LocationSensorBridge.kt` (行 459-464, 548-552)
* **問題描述**：在狀態預測階段，每一週期都累加過程雜訊到速度協方差（`p22 += qVel * dt`），但在 GPS 測量更新階段，僅更新了位置協方差，漏掉了速度協方差的更新收斂。
* **白話比喻**：系統對自己速度的「不信任感」隨著時間無限放大。運行幾分鐘後，卡爾曼增益永久逼近 1.0，整套濾波器失去平滑去噪功能，徹底退化為最原始、跳動最劇烈的 GPS 訊號。
* **改進建議**：在測量更新階段補上速度狀態的卡爾曼增益更新與協方差衰減。

### 2. 嚴重：路口狀態機永久卡死在 APPROACHING
* **相關檔案**：`app/src/main/python/web/app.js` (行 2348-2399)
* **問題描述**：狀態機邏輯為接近（6~28m）為 `APPROACHING`，通過中（<6m）為 `PASSING`，離開路口（>30m 且前一狀態為 `PASSING`）重置為 `IDLE`。若 GPS 取樣間隔稍長，使用者直接從 7m 跳至 31m（越過路口），因未踩到 `<6m` 判定點，狀態機永遠無法切換回 `IDLE`。
* **白話比喻**：系統在等著唸「正在通過路口」，但因為你跨步快了一點直接跳過去，系統就卡在「接近中」。當你走到下一個路口、下下個路口時，系統都以為你還在舊路口，後續所有路口預警全部罷工。
* **改進建議**：只要距路口距離 > 28 公尺且路口名稱改變，無論前一狀態為何，均強制復位至 `IDLE`。

### 3. 幾何變形：經度度數換算未乘上緯度餘弦值 (cos lat)
* **相關檔案**：`nmap/spatial/world_model.py`, `nmap/spatial/intersection.py`, `nmap/spatial/grid_index.py`
* **問題描述**：模組在公尺轉經緯度時，統一除以 `111139.0`。但在台灣（北緯 25 度），1 經度度的實際長度僅約 `100725` 公尺。
* **白話比喻**：拿一個正方形的網子去捕魚，但在台灣緯度上，東西向的網孔實際上被拉扯縮小了約 10%。導致明明在 45 公尺處正東方的店家或路口，被檢索框提前切掉而漏檢。
* **改進建議**：經度公尺換算一律採用 `111139.0 * cos(radians(lat))`。

### 4. 幽靈門牌：全台地址被強制拼接「新北市淡水區」
* **相關檔案**：`app/src/main/python/server.py` (行 828-830), `poi_detail_fetcher.py` (行 246-253)
* **問題描述**：在補全門牌或發起搜尋時，若地址缺少縣市前綴，代碼直接寫死：`clean_addr = f"新北市淡水區 {clean_addr}"`。
* **影響**：使用者若在台北車站、台中或高雄使用，查詢門牌時都會被冠上「新北市淡水區」，導致網路爬蟲回傳 30 公里外的錯誤電話與資訊。
* **改進建議**：由 GPS 座標反查當前真實行政區，移除寫死的淡水區字串。

### 5. 連鎖品牌冷卻碰撞：第二家 7-11 被消音
* **相關檔案**：`app/src/main/python/web/app.js` (行 2278-2285)
* **問題描述**：店家播報冷卻（35 秒）是以店名作為 Key（例如 `"7-ELEVEN"`）。
* **白話比喻**：台灣街道上兩家 7-11 相距 100 公尺十分常見。當你經過第一家時系統播報；25 秒後走到第二家門口時，因為店名一樣且未過 35 秒，系統誤以為是同一家店而直接消音。
* **改進建議**：冷卻 Key 必須綁定店家的經緯度座標或 OSM 唯一識別碼。

---

## 第三部分：可靠性 (Reliability) - 防閃退、生命週期與效能

### 1. 崩潰隱患：Android 14+ 前台服務啟動超時崩潰
* **相關檔案**：`app/src/main/java/com/example/nmapexplorer/ServerForegroundService.kt` (行 60-68)
* **問題描述**：在 Android 14 規範下，呼叫 `startForegroundService` 後必須在 5 秒內執行 `startForeground()`。現有代碼在無定位權限時直接跳過該調用，將直接引發系統強制拋出 `ForegroundServiceDidNotStartInTimeException` 導致 App 閃退。
* **改進建議**：在 `MainActivity` 確認完全取得權限後才可發起服務；若權限不足則絕不呼叫前台服務。

### 2. 嚴重漏洞：喚醒螢幕時加速度計永久脫鉤
* **相關檔案**：`app/src/main/java/com/example/nmapexplorer/LocationSensorBridge.kt` (行 915-935)
* **問題描述**：當螢幕關閉時，系統註銷了加速度計；但在螢幕亮起（`setScreenActive(true)`）時，若設備支援旋轉向量感測器，程式碼僅重新註冊了旋轉向量，**漏寫了重新註冊加速度計**。
* **後果**：視障者將手機放入口袋再拿出來後，加速度計永久罷工，計步器、Weinberg 步長模型與靜止狀態機全部癱瘓。
* **改進建議**：在喚醒邏輯中無條件補上 `TYPE_ACCELEROMETER` 的重新註冊。

### 3. 效能卡頓：門牌 O(N²) 全表比對引發主執行緒凍結 (ANR)
* **相關檔案**：`app/src/main/python/nmap/spatial/world_model.py` (行 244-254)
* **問題描述**：構建圖資時，每處理一筆門牌資料，都在已累積的 `house_numbers` 清單中執行 `any(...)` 遍歷比對。當周遭有 3,000 筆資料時，比對次數高達數百萬次。
* **改進建議**：改用 `seen_keys = set()` 雜湊集合進行查重，將比對時間由數秒縮短至數毫秒。

### 4. 閃退風險：大樓資料缺乏緯度鍵值 (Null Pointer)
* **相關檔案**：`app/src/main/python/nmap/spatial/world_model.py` (行 632-645)
* **問題描述**：搜尋鄰近大樓時使用 `bldg.get("lat")`，但構建大樓資料庫時存入的鍵名為 `center_lat`，導致回傳 `None`。隨後傳入 `haversine_distance` 時觸發 `TypeError` 導致後端拋錯中斷。
* **改進建議**：改為防禦性取值：`bldg.get("center_lat") or bldg.get("lat")`。

---

## 第四部分：建議修復優先級與升級路線圖

若後續決定進行系統升級，建議按以下四個階段推進：

### 階段一：緊急生命安全修復 (Immediate Safety Hotfix)
1. **下架虛構紅綠燈秒數**：無即時連線推播時，只報號誌不報秒數。
2. **修復防撞雷達 1.5m 盲區**：將下限改為 0.0m，極近距離觸發警報。
3. **修復人行道左右吸附顛倒**：將使用者指南針朝向同向化後再做向量外積。
4. **緊縮巷弄號誌雷達半徑**：由 32m 縮至 15m，杜絕隔壁大馬路號誌污染。

### 階段二：狀態機與感測器防死鎖 (Stability & Precision)
1. **路口狀態機防卡死**：增加遠距離或路名變更時的無條件重置為 `IDLE`。
2. **喚醒螢幕補回加速度計**：修復口袋模式取出後計步停擺問題。
3. **卡爾曼速度協方差收斂**：補上更新公式，避免定位跳動隨時間加劇。
4. **修復經度 cos(lat) 幾何公式**：消除東西向 10% 的空間檢索盲區。

### 階段三：資料健全與跨區相容 (Data Integrity)
1. **移除硬編碼「淡水區」**：改由經緯度動態判定所在城鎮與行政區。
2. **連鎖店家冷卻鍵改為座標/ID**：避免第二家同品牌超商被誤消音。
3. **大樓 center_lat 取值修復**：防範 Null Pointer 崩潰。

### 階段四：效能與 Android 系統整合 (Performance & OS Compliance)
1. **門牌 O(1) 集合查重**：解決載入大型圖資時的介面凍結。
2. **Android 14+ 前台服務生命週期加固**：防範啟動超時強制關閉。
3. **Canvas 繪圖引進髒標記 (Dirty Flag)**：靜止或暗色模式下停止每秒 60 次無效重繪，大幅延長手機電池壽命。

---

*本文件已儲存於專案根目錄，方便隨時透過螢幕報讀軟體閱讀或作為後續版本重構之工作清單。*
